import 'package:flutter/material.dart';

class AppColors {
  // Primary brand colors
  static const primary = Color(0xFF3B2FE2);
  static const primaryLight = Color(0xFF6B5FFF);
  static const primaryDark = Color(0xFF2A1FB5);
  
  // Gradient colors
  static const gradientStart = Color(0xFF3B2FE2);
  static const gradientMiddle = Color(0xFF5B4FFF);
  static const gradientEnd = Color(0xFF8B7FFF);
  
  // Accent & secondary colors
  static const accent = Color(0xFFFFC107);
  static const accentOrange = Color(0xFFFF9500);
  static const accentPink = Color(0xFFFF2D55);
  static const accentTeal = Color(0xFF00C9A7);
  static const accentCyan = Color(0xFF00D4FF);
  static const accentPurple = Color(0xFFAF52DE);
  
  // Service-specific colors (for quick links)
  static const giftcardColor = Color(0xFFFF6B6B);
  static const cryptoColor = Color(0xFFFFA726);
  static const calculatorColor = Color(0xFF26C6DA);
  static const moreServicesColor = Color(0xFF7C4DFF);
  static const airtimeColor = Color(0xFF66BB6A);
  static const dataColor = Color(0xFF42A5F5);
  static const billsColor = Color(0xFFFFCA28);
  static const cableColor = Color(0xFFEC407A);
  
  // Background colors
  static const background = Color(0xFFF5F7FA);
  static const cardBackground = Color(0xFFFFFFFF);
  static const surfaceLight = Color(0xFFF8F9FE);
  
  // Text colors
  static const text = Color(0xFF1E1E1E);
  static const textColor = Color(0xFF2D3436);
  static const textSecondary = Color(0xFF636E72);
  static const textMuted = Color(0xFFB2BEC3);
  
  // Grey scale
  static const lightGrey = Color(0xFFACB4BE);
  static const darkGrey = Color(0xFF7A7A7A);
  static const light = Color.fromARGB(255, 230, 229, 229);
  
  // Status colors
  static const success = Color(0xFF00C853);
  static const warning = Color(0xFFFFAB00);
  static const error = Color(0xFFFF1744);
  static const info = Color(0xFF2979FF);

  // Dark Mode specific colors
  static const backgroundDark = Color(0xFF0F1117);
  static const cardDark = Color(0xFF1A1D2E);
  static const surfaceDark = Color(0xFF252840);
  static const dividerDark = Color(0xFF2D3141);
  static const headerDark = Color(0xFF161929); // Dark header (replaces blue in dark mode)
  static const textDark = Color(0xFFF0F4FF);
  static const textSecondaryDark = Color(0xFFCDD1DC);
  static const textMutedDark = Color(0xFF8891A5);
}
